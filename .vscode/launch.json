{
	"configurations": [
		{
			"type": "chrome",
			"request": "launch",
			"name": "Launch",
			"url": "http://localhost:4200",
			"webRoot": "${workspaceFolder}",
			"cwd": "${workspaceFolder}",
			"preLaunchTask": "start"
		}
	]
}
